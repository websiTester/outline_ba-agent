# ─────────────────────────────────────────────────────────────
# Python Backend — RAG API (FastAPI + LightRAG + Neo4j + PostgreSQL)
# ─────────────────────────────────────────────────────────────
# Install all dependencies:
#   pip install -r requirements.txt
#
# Python version: 3.10+
# ─────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0        # ASGI server to run FastAPI
python-multipart>=0.0.9          # Required for FastAPI form/file upload endpoints

# Async file I/O
aiofiles>=24.1.0

# RAG engine (Knowledge Graph + vector search)
# Includes: lightrag.llm.gemini, lightrag.utils, lightrag.base, lightrag.kg.*
# Pinned version: ExtendedLightRAG uses workspace field (added in 1.4.9.11)
# for multi-tenant isolation. Breaking changes in future versions may require adapter updates.
lightrag-hku>=1.4.9.11,<1.5.0

# Environment variable loading (.env file)
python-dotenv>=1.0.0

# Numerical computing (used for embedding arrays)
numpy>=1.26.0

# Database ORM (async SQLAlchemy for PostgreSQL)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0                  # Async PostgreSQL driver (postgresql+asyncpg://)

# pgvector Python client (needed by LightRAG PGVectorStorage)
pgvector>=0.3.0
